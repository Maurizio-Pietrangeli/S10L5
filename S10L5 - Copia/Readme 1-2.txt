Le librerie che vengono importate dal file eseguibile (Malware_U3_W2_L5.exe) sono:

1 - KERNEL32.dll che contiene le funzioni principali per interagire con il sistema operativo.

2 - WININET.dll che contiene le funzioni per l'implementazione di alcuni protocolli di rete come HTTP, FTP e NTP.



Le sezioni di cui si compone il file eseguibile del malware sono: 

1 - .text che contiene le istruzioni che la CPU esegue una volta che il software sarà avviato.

2 - .rdata che include generalmente tutte le informazioni circa le librerie e le funzioni importate ed esportate 
           dall' eseguibile.

3 - .data che contiene i dati/le variabili globali del programma eseguibile che devono essere disponibili da           qualsiasi parte del programma.  

